#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Describes the function
 * body -program that prints all single digit numbers of base 10 startin from 0
 * Return: zero for success
*/

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%d\", n);
	}
return (0);
}
