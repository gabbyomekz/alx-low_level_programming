#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Declares function
 * body - Follow instructions to do task
 * printf or putchar - To print
 * Return: 0 exits successfully
*/
int main(void)
{
	char ch;

	for (ch = 'z'; ch >= 'a'; ch--)
	{
	putchar(ch);
	}
putchar(10);
return (0);
}
