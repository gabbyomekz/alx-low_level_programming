#include <stdio.h>
#include <stdlib.h>

/**
 * main -> Prints the name of the file it was compiled from
 * folllwed by a new line
 * This code requires the use of predefined macro
 * Return: Always 0
 */

int main(void)
{
printf("%s\n", __FILE___);

return (0);
}
