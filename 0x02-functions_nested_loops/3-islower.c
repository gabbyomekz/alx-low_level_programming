#include "main.h"

/**
 * _islower - Describes a function to check for lowercase characters
 * @c: Describes character argument
 * Return: 0 or 1 depending on the outcome
 */

int _islower(int c)
{
return (c >= 'a' && c <= 'z');
